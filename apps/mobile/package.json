{
  "name": "@asset-manager/mobile",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  }
}
